# Server Configuration
NODE_ENV=production
PORT=5000

# MongoDB Connection
# Use MONGO_DB (not MONGO_URI) - this is the env var name used by config/db.js
MONGO_DB=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/idd-job-platform

# JWT Authentication
JWT_SECRET=idd_job_platform_jwt_secret_2026_secure_key_change_me
JWT_EXPIRE=30d

# ============================================================================
# CORS Configuration
# ============================================================================

# CORS_ORIGINS: Comma-separated list of allowed frontend origins
# Default (if not set): https://iddjobplatform.vercel.app,https://idd-job-platform.vercel.app
# Example for multiple origins:
# CORS_ORIGINS=https://myapp.vercel.app,https://myapp-staging.vercel.app,https://custom-domain.com
CORS_ORIGINS=https://iddjobplatform.vercel.app,https://idd-job-platform.vercel.app

# ALLOW_LOCALHOST_ORIGIN: Explicitly allow http://localhost:3000
# Set to 'true' when testing local frontend against hosted backend (e.g., Render)
# Safe for development/staging, but not needed in production if frontend is also deployed
# Default: not set (localhost blocked in production)
ALLOW_LOCALHOST_ORIGIN=false

# ALLOW_ALL_ORIGINS: Emergency override - allows ANY origin to access the API
# ⚠️ WARNING: This is a SECURITY RISK - only use for temporary debugging!
# ⚠️ NEVER enable this in production!
# Set to 'true' only when troubleshooting CORS issues locally
# Default: not set (restricted CORS)
ALLOW_ALL_ORIGINS=false

# ============================================================================
# Configuration Examples
# ============================================================================

# Local Development (backend on localhost:5000, frontend on localhost:3000):
# NODE_ENV=development
# ALLOW_ALL_ORIGINS=true
# (or just rely on default behavior - local dev usually works without restrictions)

# Render Staging (backend hosted, frontend on localhost for testing):
# NODE_ENV=production
# ALLOW_LOCALHOST_ORIGIN=true
# CORS_ORIGINS=https://iddjobplatform.vercel.app

# Production (both backend and frontend deployed):
# NODE_ENV=production
# ALLOW_LOCALHOST_ORIGIN=false
# CORS_ORIGINS=https://iddjobplatform.vercel.app,https://idd-job-platform.vercel.app
# ALLOW_ALL_ORIGINS=false
